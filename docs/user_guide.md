# 📖 User Guide: AI PM Roadmap Agent

### 1. Preparing the Google Sheet
- Use the provided template with columns: `Date`, `Source`, `Segment`, `Raw Feedback`.
- **Crucial**: Share your sheet with the service account email found in `credentials.json`.

### 2. Launching the App
- Double-click `run_my_app.bat`.
- A browser window will open at `http://localhost:8501`.

### 3. Generating the Roadmap
- Click the **"Update Roadmap Now"** button.
- The agent will cluster the feedback and write the results to a new tab named `Roadmap Output`.